apiVersion: skaffold/v2beta26
kind: Config
metadata:
  name: deliverys-service

build:
  artifacts:
    - image: delivery-service
      context: .
      docker:
        dockerfile: Dockerfile

deploy:
  kubectl:
    manifests:
      - ./configuration/secrets.yaml
      - ./configuration/deployment.yaml
      - ./configuration/service.yaml
      - ./configuration/database.yaml

portForward:
  - resourceType: service
    resourceName: delivery-service
    port: 80
    localPort: 8000
  - resourceType: service
    resourceName: delivery-postgres
    port: 5432
    localPort: 5432